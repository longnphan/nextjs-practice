import { useRouter } from "next/router";

const BlogPage = () => {
    const router = useRouter();

    return (
        <div>
            <h1>Blog Posts Page</h1>
        </div>
    );
};

export default BlogPage;
